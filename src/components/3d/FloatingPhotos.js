// Floating memory frames are now part of the persistent WorldRenderer.
// Real photos continue to be rendered by the Memories section above the canvas.
export class FloatingPhotos {
  constructor() {}
  update() {}
  destroy() {}
}

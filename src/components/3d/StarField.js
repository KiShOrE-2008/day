// Star rendering is now part of the persistent WorldRenderer.
// This compatibility class is retained so existing imports remain safe.
export class StarField {
  constructor() {}
  update() {}
  destroy() {}
}
